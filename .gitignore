# Виртуальное окружение (venv, .venv, env)
venv/
.venv/
env/
ENV/

# Файлы зависимостей (обычно устанавливаются в venv, но иногда могут быть в проекте)
*.py[cod]
__pycache__/
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
*.egg
*.manifest
*.spec

# Pip-зависимости (если они хранятся локально)
pip-selfcheck.json
pip-log.txt

# PyCharm / IDE
.idea/
.vscode/
*.swp
*.swo

# Файлы окружения (env-файлы с секретами)
.env
.env.local
.env*.local
__pycache__/
*.py[cod]
*$py.class
# Игнорировать все файлы .env и их вариации
.env
.env.*
!.env.example  # если нужно исключить какой-то файл (например, шаблон)